services:
  tux-letter:
    build: .
    container_name: tux-letter
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/tux-letter.db
